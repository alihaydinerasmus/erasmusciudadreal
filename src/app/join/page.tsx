import { JoinPageClient } from "@/components/JoinPageClient";

export const metadata = {
  title: "Join — Memory Album",
  robots: { index: false, follow: false },
};

export default function JoinPage() {
  return <JoinPageClient />;
}
