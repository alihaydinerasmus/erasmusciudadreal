import { EnterPageClient } from "@/components/EnterPageClient";

export const metadata = {
  title: "Enter — Ciudad Real",
  robots: { index: false, follow: false },
};

export default function EnterPage() {
  return <EnterPageClient />;
}
